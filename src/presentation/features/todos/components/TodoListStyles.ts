import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listContent: {
    paddingBottom: 20,
  },
  footerLoader: {
    paddingVertical: 16,
    alignItems: "center",
  },
});
